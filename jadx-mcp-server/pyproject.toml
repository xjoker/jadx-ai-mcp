[project]
name = "jadx-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.28.1",
]

[tool.setuptools]
py-modules = ["jadx_mcp_server"]

[project.scripts]
jadx_mcp_server = "jadx_mcp_server:main"
